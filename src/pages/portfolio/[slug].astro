---
import ContentLayout from '../../layouts/ContentLayout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const portfolio = await getCollection('portfolio');
  return portfolio.map((item) => ({
    params: { slug: item.slug },
    props: { item },
  }));
}

const { item } = Astro.props;
const { Content } = await render(item);

const langColors: Record<string, string> = {
  python: '#4FA8E0',
  tensorflow: '#FF8A00',
  keras: '#FF4D6B',
  jax: '#7EC832',
  streamlit: '#FF4D6B',
  javascript: '#FFD700',
  html5: '#FF8A00',
  css3: '#4FA8E0',
  maths: '#B47FFF',
  idyll: '#7EC832',
  default: '#9090a0',
};

function getLangColor(lang: string): string {
  return langColors[lang.toLowerCase()] || langColors.default;
}
---

<ContentLayout
  title={item.data.title}
  description={item.data.twitter_summary || `Portfolio project: ${item.data.title}`}
  accentColor="port"
  backHref="/portfolio"
  backLabel="Portfolio"
>
  <!-- Header -->
  <div class="mb-8">
    <h1 class="text-3xl sm:text-4xl font-bold text-white mb-3">{item.data.title}</h1>
    <p class="text-sm mb-4" style="color: #9090a0;">
      {item.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
    </p>

    {item.data.languages && item.data.languages.length > 0 && (
      <div class="flex flex-wrap gap-2 mb-4">
        {item.data.languages.map((lang) => (
          <span
            class="tag text-sm"
            style={`background: rgba(${parseInt(getLangColor(lang).slice(1,3), 16)}, ${parseInt(getLangColor(lang).slice(3,5), 16)}, ${parseInt(getLangColor(lang).slice(5,7), 16)}, 0.12); color: ${getLangColor(lang)};`}
          >
            {lang}
          </span>
        ))}
      </div>
    )}

    <!-- Links -->
    <div class="flex flex-wrap gap-3">
      {item.data.github && (
        <a
          href={item.data.github}
          target="_blank"
          rel="noopener"
          class="flex items-center gap-2 px-3 py-1.5 rounded-lg text-sm font-medium transition-all hover:opacity-80"
          style="background: rgba(180, 127, 255, 0.15); color: #B47FFF; border: 1px solid rgba(180, 127, 255, 0.3);"
        >
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          GitHub
        </a>
      )}
      {item.data.kaggle && (
        <a
          href={item.data.kaggle}
          target="_blank"
          rel="noopener"
          class="flex items-center gap-2 px-3 py-1.5 rounded-lg text-sm font-medium transition-all hover:opacity-80"
          style="background: rgba(79, 168, 224, 0.15); color: #4FA8E0; border: 1px solid rgba(79, 168, 224, 0.3);"
        >
          Kaggle
        </a>
      )}
      {item.data.colab && (
        <a
          href={item.data.colab}
          target="_blank"
          rel="noopener"
          class="flex items-center gap-2 px-3 py-1.5 rounded-lg text-sm font-medium transition-all hover:opacity-80"
          style="background: rgba(255, 138, 0, 0.15); color: #FF8A00; border: 1px solid rgba(255, 138, 0, 0.3);"
        >
          Open in Colab
        </a>
      )}
    </div>
  </div>

  <!-- Thumbnail -->
  {item.data.thumb && (
    <div class="mb-8 rounded-xl overflow-hidden" style="border: 1px solid rgba(255,255,255,0.08);">
      <img src={item.data.thumb} alt={item.data.title} class="w-full max-h-64 object-cover" />
    </div>
  )}

  <!-- Content -->
  <Content />
</ContentLayout>
